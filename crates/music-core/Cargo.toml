[package]
name = "music-core"
version = "0.1.0"
edition = "2024"
rust-version.workspace = true

[lib]
name = "music_core"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
