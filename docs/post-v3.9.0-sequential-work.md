# v3.9.0後の直列開発計画

2026-09-13公開前追補: §1.233のCtrl+上下フォルダ移動ちらつきが判明したため公開を保留し、先に修正する。従来項目の完了判定は維持するが、公開可能という総合判断はいったん保留した。同日、修正・自動検証・独立レビュー・利用者確認が完了し、利用者がClaudeCodeでの公開再開を希望。[調査と修正記録](fullscreen-folder-sidecar-transition-investigation-20260913.md)。

2026-09-12利用者指定。以下の直列順を維持する。
同日追加判断により、9月13日公開予定のv3.9.1は§1.226の操作待ち・復元表示修正と
完了済みの依存更新で区切る。§1.223以降はv3.9.1に追加せず、後続版へ延期する。
重複指定された§1.222は1件として扱う。

2026-09-12、v3.9.1公開作業中の追加指定: 次リリースでは§1.228（変更の無いZIP/PDFを
起動時の類似索引確認で開かない改善）を最優先にする。§1.228 → §1.223 → §1.222 →
§1.218 → §1.212 → §1.164 → §1.221 → §1.220の順とし、v3.9.1には追加しない。
v3.9.1公開完了の利用者連絡を受け、§1.228からの順次実装を開始する。
§1.228は実装・全体テスト・独立レビュー・確認build完了。9月13日の利用者起動ログではInitial Full約42秒（旧観測391秒）、後続は短いDeltaのみ。
[検証記録](section228-similar-container-preopen-plan.md)を参照。§1.223は`49c621ab8`、§1.222は`a7b0504fa`、§1.218は`e35b12a89`で実装。§1.223・§1.212の追補は`1906f2936`で2026-09-13利用者確認済み。§1.164は`3a1028647`、§1.221は`9a5544e29`で実装・自動検証・確認buildを完了。§1.220も実装・自動検証・確認build・利用者確認を終え、`cd5a3a848`へcommit済み。
起動時のInitial Fullだけ索引を開く前に再利用判定し、
Reconfigure（パスワード変更を含む）/修復/手動Fullと監視Deltaは従来どおり列挙後に判定する。

| 順序 | 対象 | 現在の状態 |
| --- | --- | --- |
| 1 | §1.115 別窓静止画の開閉ちらつき | 完了。旧連続ちらつきは非再現。追加の最大化時引伸ばしは§1.227として保留・次版除外（[判定記録](section115-revalidation-plan.md)） |
| 2 | PDFium・FFmpeg更新 | 完了。PDFium 155.0.8044.0へ更新、FFmpegは現7.1系を維持。全体直列gate・独立レビュー・利用者PDF/動画確認済み。先行並列snapshot異常の原因は未確定（[検証記録](dependency-update-20260912.md)） |
| 3 | §1.223 比較ワイプ開始時の操作案内 | 常時境界表示・Ctrl押下中のみ非表示を実装し、対象検証・独立レビュー・確認build完了。2026-09-13改修後も利用者確認済み |
| 4 | §1.222 viewer context audit CI | 実装・監査35件・repository audit成功。製品コード不変、直前の全体gate/buildを再利用 |
| 5 | §1.218 先頭・末尾の単ページを本来の側に配置 | 実装・全体テスト・監査・独立レビュー・確認build完了（main 8323/0/45）。2026-09-13本体・Remoteとも利用者確認済み。[検証記録](section218-singleton-spread-placement.md) |
| 6 | §1.212 通常動画の縮小下限 | 縮小中のドラッグとresize後の移動、V中の左右パネル抑止・上下HUD維持を実装。対象検証・独立レビュー・確認build完了。2026-09-13利用者実機確認済み |
| 7 | §1.164 修飾キーで切り取り枠を新規作成 | 実装・独立レビュー・全体テスト（main 8337/0/45）・確認build完了。2026-09-13利用者実機確認済み。[検証記録](section164-force-new-crop-frame.md) |
| 8 | §1.221 右クリックメニュー表示・並べ替え | 初回と区切り線追補は検証・commit済み。追加の表示場面確認・区切り線2値表示・設定ページ分離も実装・独立レビュー・全体gate（8382/0/45、snapshot 50/50）完了。20:18の確認build完了。2026-09-13追加指定分も利用者から動作問題なしの確認済み。[検証記録](section221-context-menu-layout.md) |
| 9 | §1.220 切り取り中の実ファイル・フォルダの半透明表示 | Explorer読取・ハサミ追補を含め実装・独立レビュー・全体テスト（main 8378/0/45、snapshot 50/50）・確認build・利用者確認完了。`cd5a3a848`へcommit済み。[記録](section220-cut-item-appearance.md) |

親が各項目の範囲・不変条件・完了条件を整理し、Sol/xhigh実装担当へ一まとまりで渡す。
独立レビューは別のSol/xhigh担当。ひとつの項目の調査・実装・検証を完了させてから次へ進む。
同時編集とCargoの重複を避け、成功済み検証は対象・条件が有効な範囲で再利用する。
未コミットの他系統変更は保持する。公開準備・公開は従来どおりClaudeCode Opus担当。

2026-09-12追加: §1.226（初訪問時のサイドカー確認を「復元中」と表示する誤解）も次版対象。
利用者了承により旧XMP自動タグ移行の廃止と表示改善を一まとまりで対応する。
依存更新の区切り後に着手し、元の項目同士の順序は維持する。
§1.226は実装・対象回帰・全体gate・確認用build・独立レビューを完了（`39390ec77`）。
2026-09-12、利用者から確認用buildで「大丈夫そう」との確認を受け、開発側の対応を完了した。
全体gateは当該実行のみ`RUST_TEST_THREADS=1`でexit 0（main 8295 passed / 0 failed / 45 ignored）。
`build-dev.ps1 -PreserveRuntime`で通常プロファイル用core/remoteを生成し、稼働中アプリは停止していない。
証跡は`target/section226-legacy-xmp-retirement-20260912/`。

### v3.9.1公開担当への引き継ぎ

- 含めるのは§1.226とPDFium 155.0.8044.0更新。FFmpegは確認の結果、現7.1系を維持。
- §1.223以降・§1.227は含めない。公開日は9月13日予定から、準備が整えば9月12日も検討する利用者意向。
- §1.226確認core SHA-256: `7C66775236892C369957B59192467264E26A84BFE783B64179DA70B71E75617B`。
  利用者自身によるhash照合や、修正後の秒数計測は行っていない。
- PDF/動画の利用者確認と依存更新の証跡は[依存更新記録](dependency-update-20260912.md)。
- 今回の開発完了は公開前のperf smoke / idle-healthや配布物確認の免除ではない。
  公開担当が既存手順に従い、実機操作は対象・所要時間を示して利用者了承後に実施する。
- 既知の問題ページは`2eab9c7c2`でv3.9.0の問題を告知済み。v3.9.1公開時に解消済みへ整合させる。
- 残る未コミット`src/app/tests/detached_binding_identity.rs`と`src/gpu_anime4k_generated.rs`は他所有の既存変更。
  今回の修正には含めず保持した。

## §1.115の開始条件

古い仮説が複数回訂正されているため、現sourceと履歴を照合してから再確認手順を作る。
特にタイトルバー最大化とF11を区別し、Enterによる明示close/open、font lifecycle撤去済み範囲、
hostのvisibility/ready/placementと入力所有権を確認する。見た目が再現しないだけで構造的な
完了条件を満たしたとは扱わず、追加実装が必要かは証拠と独立レビューで判断する。
detached-rework-plan §2を維持し、症状guard・delay・追加repaint・機能制限で置き換えない。
実機操作は手順・所要時間・使い捨てdata・入力使用を具体化し、利用者了承後のみ。
通常APPDATAを使う必要がある操作は利用者自身に依頼し、agentは通常アプリを起動・停止しない。
ユーザー確認が必要になった場合は、後続項目を勝手に並行実装せず判断を求める。

## 現在の確認用buildで利用者に確認を依頼する項目

最新状態: §1.221表示場面preview追補は実装・独立レビュー・自動検証・確認build完了。利用者の終了連絡後、2026-09-13 20:18 JSTに`build-dev.ps1 -PreserveRuntime`がexit 0。core SHA-256は`8F867A41B3F20E9AB57E2F80DD90FA82B4809720D35808AC52237DC48397BF4E`。全体gateはmain 8382/0/45、snapshot 50/50。検証ログとsource manifestは`target/section221-scene-preview-20260913/`。通常アプリをagentは起動・停止していない。以下の18:36記録は追補前の履歴。

2026-09-13追補確認: 利用者から「切り取り表示は良さそう」との確認を受け、§1.220の受入・commit保留を解除する。以下の未確認表記は確認前の経過。§1.221には区切り線既定の2値表示、表示場面の明示、通常/右クリック設定の分割が追加指定され、別途対応する。

2026-09-13実機追補: §1.221の不要な上下ボタン撤去と区切り線設定（`0e6db99a9`）、§1.220のExplorer Cut非反映とハサミ表示を実装・独立レビュー・自動検証済み。18:36の確認buildへ更新した。mIV内Cutの半透明化は利用者確認済みだが、§1.220全体の完了・commitは修正後の実Explorer再確認まで保留する。

- §1.164: 切り取り枠の内部・枠線・ハンドルからCtrlドラッグで新しい枠を作れること。
  Spaceによる画像移動と通常の枠編集も維持されること。自動検証済み。2026-09-13、利用者から§1.164の確認済み・動作問題なしの連絡を受けた。
- §1.221: 環境設定「表示 → 通常メニュー」と「表示 → 右クリックメニュー」が分かれていること。
  右クリックの表示場面を画像と通常フォルダの余白などで切り替え、対象外の項目が判別でき、
  区切り線が実際の表示/非表示で示されること。項目の非表示・上下移動と既定への復帰も確認する。
  表示場面の切替自体では保存設定が変わらないこと。自動検証済み。2026-09-13、追加指定分も利用者から動作問題なしの確認済み。
- §1.220: Explorer切り取り対応とハサミ表示を含め、利用者確認済み。

2026-09-13 18:36 JSTの確認coreは上記3項目と実機追補を含む。
`target/dev-runtime/mimageviewer-core.exe` のSHA-256は
`6910EAE6ACB511D9C1BF4C77ABA673C087B9624F27CBEC99E9F978375E7A5002`。
全体gateはexit 0（main 8378/0/45、snapshot 50/50）、実行ログは `target/section220-221-followup-20260913/`。17:05以前の検証記録は各設計書に区別して保持する。
§1.220はWindows実機確認後に`cd5a3a848`へcommit済み。他所有の既存Rust 2ファイルは引き続き含めない。

これらはagentによる通常APPDATAアプリの起動や実クリップボードの書き換えを許可するものではない。
agentの実機検証は使い捨て環境・操作内容・所要時間を提示して個別了承を得る。

## 後続版の候補

§1.118コレクションは順序付き集合と順序なしを選べるようにする。UIは要検討。
スライドショーと動画連続再生はコレクションに属する項目間で移動する。
初期import/export要件を維持する。§1.223〜§1.220の直列実装後に検討し、コレクションには着手しない。

## §1.233公開前修正の完了（2026-09-13）

サイドカー復元のexact再open要求を既存navigation待機へ統合。全体gateは本体8386成功・失敗0・ignored45、snapshot50成功。確認buildはexit 0で23:40:26 JSTのcore SHA-256は`501D23D1E8620719AD82A57189E7E7FAF043745E127EE2DD268081E89BAF72B2`。証拠は`target/section233-fullscreen-sidecar-transition-20260913/`。独立担当も最終ソース・全体gate・buildを照合し承認済み。利用者から「なおりました。よさそうです」と修正版の動作確認を受けた。§1.233を理由とする公開保留を解除し、ClaudeCodeの公開準備へ引き継ぐ。最終配布物のリリースゲートは公開担当が実施する。
