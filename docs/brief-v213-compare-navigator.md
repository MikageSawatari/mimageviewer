# ブリーフ: ワイプ比較の境界ずれ (§1.60) とナビゲータの残り 2 件 (§1.55)

対象: v2.13.0 出荷スコープ。実装 = Codex Sol / レビュー・検収 = ClaudeCode /
実機確認 = 利用者。

正本: [docs/next-release-backlog.md](next-release-backlog.md) §1.60 / §1.55。
**原因と方針は調査済み**なので、そのとおりに実装すること。

3 つとも**比較表示の描画経路**を触るのでまとめて出す。ただし
**§1.60 / §1.55-1 / §1.55-2 の変更は分離できる形**にすること (ClaudeCode が別コミットにする)。

前提: master の作業ツリーで作業する。着手前に `git log --oneline -3` で HEAD を確認すること。

---

## 1. §1.60 ワイプ比較でズームすると境界線と掴み位置がずれる (バグ・先に直す)

**原因は特定済み**: **描画と入力で別の矩形を基準にしている**。

- 描画: [ui_fullscreen.rs:21112 付近](../src/ui_fullscreen.rs) の `draw_compare_wipe_line` は
  `draw_rect` (= `compare_image_draw_rect` が返す、fit とズーム・パンを反映した実表示画像矩形)
  を基準にする。シェーダの合成境界も同じ `draw_rect` 内で決まる
- 入力: [`handle_compare_wipe_drag`](../src/ui_fullscreen.rs:14628) は `image_rect`
  (= ビューポート全体) を基準に `line_x` と `new_fraction` を計算している
- 等倍かつ画像が画面幅いっぱいのときだけ一致するので表面化しなかった。ズームすると
  `draw_rect` だけが動いてずれ、掴むと `image_rect` 基準で再計算されるため境界が跳ぶ。
  **レターボックスが出る縦横比ではズームなしでもずれる**

**直し方**: **合成境界・線の描画・掴み判定を同じ矩形から導く**。
`compare_image_draw_rect` は `image_rect` / `pair.target_size` / `zoom_pan` / fit 上限から
決まる純粋な計算なので、入力ハンドラ側でも**同じ値を求めて使う**。

- **前フレームの `draw_rect` を保存する方式は採らないこと** (ズーム中に 1 フレーム遅れる)
- 掴み判定の範囲も `image_rect` ではなく `draw_rect` に合わせる
- fraction の意味 (0..1 が `draw_rect` の左端〜右端) を 1 箇所に決め、描画・シェーダ・入力の
  3 者が同じ定義を使っていることがコード上で分かる形にする

**テスト**: `draw_rect` を与えて fraction ↔ 画面 x を往復する純関数を固定する。
等倍・ズーム時・レターボックス時の 3 条件で、**描画に使う線の x と、掴み判定が同じ
fraction を返す x が一致**すること。

---

## 2. §1.55-1 ナビゲータを画像全体が見えているときも表示する

現在は [ui_fullscreen.rs:475](../src/ui_fullscreen.rs) で「全ページが完全に見えているなら
枠が全面になって情報がないので隠す」としている。**隠さない方針に変える** (利用者決定)。
ナビゲータ上のドラッグ範囲指定ズームは、等倍表示のときこそ使いたい操作のため。

- 表示範囲が画像より広いときは、**枠をナビゲータ全域に置き、画像側を内側へ縮めて描く**
  (利用者決定)。「今は画像全体より広く見えている」ことが読み取れる形にする
- 画像の縮小には**下限を設ける** (例: キャンバスの 40%)。倍率へ完全連動させると、
  ズームアウトのたびに絵が縮んで用をなさなくなる
- **フィット丁度では画像 = 枠 = 全域で連続する**こと。ここが不連続だと、ズーム操作中に
  絵の大きさが飛んで見える。**この連続性をテストで固定すること**

## 3. §1.55-2 比較表示 (X / C) 中もナビゲータを出す

現在は [ui_fullscreen.rs:14693](../src/ui_fullscreen.rs) の
`matches!(self.compare_view_mode, CompareViewMode::Off)` で明示的に切っている。

- 出す絵は「**いま画面に出ているのと同じ合成結果の全体像**」(利用者決定)
- **新しいシェーダは不要**。[`compare_shader_shape`](../src/ui_fullscreen.rs:20297) は
  コールバックへ両画像の全体 RGBA を渡し、`draw_rect` へ画像全体を写す作りなので、
  **`image_rect` にナビゲータのパネル矩形、`zoom_pan` に `None` (フィット) を渡して
  再度呼ぶ**だけで同じ合成が全体像として得られる。`Wipe` / `Diff` は同経路、
  `PinnedNormal` だけは準備済みテクスチャを直接フィット描画する
- **注意**: 1 フレームに合成パスが 2 回走る。`CompareShaderCallback` が `pair.key` で
  GPU 側のテクスチャを再利用するかを**確認すること** (再利用しないなら 2 回アップロードに
  なるので、そこは報告して判断を仰ぐ)。Shape はナビゲータのクリップ矩形へ乗せる
- §1.60 を先に直してから着手すること (同じ `draw_rect` の定義を使うため)

---

## 4. やらないこと

- §1.59 (魚眼投影) は**今回の対象外**。触らない
- ナビゲータの既存のドラッグ範囲指定ズーム、表示位置、サイズ設定を変えない
- マウス / キーボードの既存挙動を変えない
- 症状パッチ (前フレーム値の保存、しきい値ガード、1 フレーム遅延の許容) を入れない

## 5. 完了条件

- `cargo fmt` (引数なし)
- `cargo test -p mimageviewer --lib` が全件 / `cargo test -p mimageviewer --test ui_snapshot`
- `cargo check -p mimageviewer --bin mimageviewer-core`
- `python scripts/check_ui_glyphs.py` が 0 件
- 非 Windows を壊さないこと
- **バックログ §1.60 / §1.55 へ実装記録を追記**
- ナビゲータの表示条件が変わるので `htdocs/mimageviewer/manual/` の該当箇所を更新する

## 6. 制約

- **アプリを起動しないこと。** 検証ビルドと実機依頼は ClaudeCode が行う
- **ブランチ操作・コミットをしないこと。** master の作業ツリーで未コミットのまま残す
- 判断が要る事態になったら、その項目だけ止めて報告し、残りは進めてよい

---

完了したら次を報告すること (項目ごとに):

1. 直した構造と、描画・入力が同じ値を使っていることの根拠
2. §1.55-2 の合成パス 2 回分のコスト (テクスチャ再利用の有無)
3. テスト結果
4. **実機で確認してほしいこと**
