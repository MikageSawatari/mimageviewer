# v3.5.0 再レビュー (第7回)

開始: 2026-09-03。基点 `6cd99d91596189f82a4601ba85d89209d715ced7` の作業ツリー差分。
途中で同じ内容が `8868b4378` としてコミットされた。対象 9 files / +353 -34。
**再レビュー完了。T01 / T02 は解消を確認し、新規 U01 (P2: 一括 Undo の反復全件検索) を記録。**
全体ゲートは同時実行数 8 で完走し、**8,189 passed / 0 failed / 36 ignored**。
本体 7,265 件、UI snapshot 43 件、vendored 9 + 15 件を含む。

| 確認対象 | 状況 |
|---|---|
| Undo の記録・復元・直接構築箇所 | T01 の対象誤りは解消。大量一括 Undo / Redo の性能退行 U01 |
| 通常画像・ZIP/PDF・製本・Remote の保存先 | 共通キー / writer と capture の重複防止、標準のカスケード復元を確認 |
| 所有窓・並べ替え・終了時の HUD | T02 解消。回帰テストおよび実装抽出 probe で配送先 / 終了を確認 |
| 回帰テストと全体ゲート | owner テスト 12 件 / fmt / HUD probe / 全体ゲートは PASS。キー検索の CPU probe で U01 を確認 |
| 延期事項 | R07–R11 の判断を維持。別窓からの Undo 後、元の窓の表示が古いままになる残件は、今回の明示的な延期 (backlog §1.173) として扱う |

レビューのみ。アプリソースの修正・配布ビルド・起動・commit / push は行わない。
`docs/duplicate-detection-plan.md` の無関係な変更と過去レビューを保持する。

詳細は [findings.md](findings.md)、検証範囲は [verification.md](verification.md)。
最終 HEAD: `8868b43780a557121e285adf0ff907bf6ca0f367`。
開始時と終了時の対象ソース SHA-256 は一致。今回開始した検証プロセスはすべて終了済み。
